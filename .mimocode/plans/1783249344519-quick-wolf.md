# Plan: Integration Test for Full Import Flow

## Problem

ImportedTrades table is empty because:
1. Tests use in-memory SQLite (ephemeral)
2. The EA needs to write files for import to trigger
3. No test exercises the complete file → parse → persist flow

## Solution

Write an integration test that:
1. Creates sample JSON files matching EA output format
2. Triggers the PlatformService import methods
3. Verifies ImportedTrades records exist in the database

## Files to Create/Modify

| Action | File |
|--------|------|
| Create | `tests/Certus.IntegrationTests/FullImportFlowTests.cs` |

## Test Structure

```csharp
public class FullImportFlowTests : IClassFixture<CustomWebApplicationFactory>
{
    // Test 1: Import portfolio from JSON file → Portfolio record in DB
    // Test 2: Import trades from JSON file → ImportedTrade records in DB
    // Test 3: Import same trade twice → duplicate skipped
    // Test 4: Import with deviation calculation
}
```

## Verification

1. Run tests and verify ImportedTrades table has records
2. Check that Portfolio records are created with IsPlatformManaged = true
3. Verify duplicate detection works
